import React from 'react'

const CreateCategory = () => {
  return (
    <div>CreateCategory</div>
  )
}

export default CreateCategory

export const metadata = {
  title: 'Agora: Add Category',
  description: 'Create a new category on Agora.',
};
